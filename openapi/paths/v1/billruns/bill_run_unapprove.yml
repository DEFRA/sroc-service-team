patch:
  operationId: UnapproveBillRun
  description: "Unapproves all transactions in a specified bill run. Bill run must previously have been approved and must be in unbilled status."
  tags:
    - billrun
  parameters:
    - $ref: '../../../schema/parameters.yml#/regime'
    - $ref: '../../../schema/parameters.yml#/billrunId'
  responses:
    '204':
      description: "Success"
