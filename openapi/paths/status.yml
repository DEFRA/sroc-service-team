get:
  operationId: Status
  description: "Confirm service is up and running"
  responses:
    '200':
      description: "Success"
      content:
        application/json:
          schema:
            $ref: '../schema/schemas.yml#/status'
